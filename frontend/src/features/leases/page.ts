
import { leaseRepository } from "../../repositories/leaseRepository";
import { createTable } from "../shared/table";
import { currency, escapeHtml } from "../shared/format";
export async function renderLeases(container:HTMLElement):Promise<void>{const rows=await leaseRepository.getListItems(); container.innerHTML=`<div class="page-heading"><h1>Leases</h1><p class="text-body-secondary mb-0">IndexedDB leases with one or more participants</p></div><div class="card"><div class="card-body"><table id="leases-table" class="table table-hover align-middle w-100"><thead><tr><th>Leaseholders</th><th>People</th><th>Street</th><th>Apartment</th><th>Start</th><th>End</th><th>Monthly Rent</th><th>Status</th></tr></thead></table></div></div>`; createTable("#leases-table",{data:rows,columns:[{data:"leaseholders",render:(v:string[])=>v.map(escapeHtml).join("<br>")},{data:"leaseholders",render:(v:string[])=>v.length},{data:"street"},{data:"apartment"},{data:"startDate"},{data:"endDate"},{data:"monthlyRent",render:(v:number)=>currency(v)},{data:"status",render:(v:string)=>`<span class="badge text-bg-success">${escapeHtml(v)}</span>`}]});}
