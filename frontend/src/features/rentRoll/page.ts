
import { rentLedgerService } from "../../services/rentLedgerService";
import { createTable } from "../shared/table";
import { currency } from "../shared/format";
const currentPeriod=()=>new Date().toISOString().slice(0,7);
export async function renderRentRoll(container:HTMLElement){const period=new URLSearchParams(location.hash.split("?")[1]??"").get("period")??currentPeriod();const rows=await rentLedgerService.getRentRoll(period);container.innerHTML=`<div class="page-heading d-flex justify-content-between align-items-end"><div><h1>Rent Roll</h1><p class="text-body-secondary mb-0">Current month plus all prior unpaid rent.</p></div><div><label class="form-label">Rent Period</label><input type="month" id="rent-period" class="form-control" value="${period}"></div></div><div class="row g-3 mb-4">${card("Expected This Month",rows.reduce((t,r)=>t+r.currentMonthDue,0))}${card("Paid This Month",rows.reduce((t,r)=>t+r.currentMonthPaid,0))}${card("Prior Arrears",rows.reduce((t,r)=>t+r.priorBalance,0))}${card("Total Outstanding",rows.reduce((t,r)=>t+r.totalOutstanding,0))}</div><div class="card"><div class="card-body"><table id="rent-roll-table" class="table table-hover align-middle w-100"><thead><tr><th>Unit</th><th>Primary Tenant</th><th>Current Due</th><th>Current Paid</th><th>Prior Balance</th><th>Total Outstanding</th><th>Oldest Unpaid</th><th>Months Behind</th><th>Status</th><th>Action</th></tr></thead></table></div></div>`;createTable("#rent-roll-table",{data:rows,columns:[{data:"unitLabel"},{data:"primaryTenant"},{data:"currentMonthDue",render:(v:number)=>currency(v)},{data:"currentMonthPaid",render:(v:number)=>currency(v)},{data:"priorBalance",render:(v:number)=>currency(v)},{data:"totalOutstanding",render:(v:number)=>currency(v)},{data:"oldestUnpaidPeriod",render:(v:string)=>v||"—"},{data:"monthsInArrears"},{data:"status",render:(v:string)=>`<span class="badge text-bg-${v==="Current"?"success":v==="Partial"?"warning":"danger"}">${v}</span>`},{data:"leaseId",orderable:false,searchable:false,render:(id:number)=>`<a class="btn btn-sm btn-outline-primary" href="#/payments/new?leaseId=${id}">Record Payment</a>`}]});document.getElementById("rent-period")?.addEventListener("change",e=>location.hash=`#/rent-roll?period=${(e.target as HTMLInputElement).value}`)}
function card(label:string,value:number){return `<div class="col-sm-6 col-xl-3"><div class="card h-100"><div class="card-body"><div class="small text-uppercase text-body-secondary fw-semibold">${label}</div><div class="metric-value">${currency(value)}</div></div></div></div>`}
